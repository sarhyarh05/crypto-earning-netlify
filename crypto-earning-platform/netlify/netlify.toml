[build]
  # No build step needed for the static front‑end
  publish = "client"
  # Functions live in netlify/functions
  functions = "netlify/functions"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
